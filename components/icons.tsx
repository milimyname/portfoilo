import { CopyIcon, SidebarClose, SmileIcon } from "lucide-react"
import type { Icon as IconType } from "lucide-react"

export type Icon = IconType

export const Icons = {
  copy: CopyIcon,
  smile: SmileIcon,
  menu: SidebarClose,
}
